import { ComentariosPipe } from './comentarios.pipe';

describe('ComentariosPipe', () => {
  it('create an instance', () => {
    const pipe = new ComentariosPipe();
    expect(pipe).toBeTruthy();
  });
});
