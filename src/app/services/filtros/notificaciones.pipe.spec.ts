import { NotificacionesPipe } from './notificaciones.pipe';

describe('NotificacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
